import Foundation

enum AppConfig {
    static let appName = "Mento AI"
    static let bundleIdentifier = "com.ataberksalar.mentoai"
    static let homeURL = URL(string: "https://mento-ai.com/panel/bugunku-plan")!
    static let fallbackURL = URL(string: "https://mento-ai.com")!
    static let allowedHost = "mento-ai.com"
}
